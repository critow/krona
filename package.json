{
  "name": "krona-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "dev": "pnpm --filter krona-playground dev",
    "test": "vitest run --project core",
    "test:watch": "vitest",
    "test:browser": "vitest run --project browser",
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots",
    "bench": "vitest bench --run --project core",
    "typecheck": "tsc --build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "check-deps": "node scripts/check-deps.mjs",
    "verify": "pnpm lint && pnpm check-css && pnpm check-docs && pnpm typecheck && pnpm test:all && pnpm build",
    "test:all": "vitest run",
    "build:css": "node scripts/build-css.mjs",
    "check-css": "node scripts/build-css.mjs --check",
    "check-docs": "node scripts/check-docs.mjs"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@biomejs/biome": "catalog:",
    "@playwright/test": "catalog:",
    "@types/node": "^26.2.0",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "@vitest/browser": "catalog:",
    "@vitest/browser-playwright": "^4.1.11",
    "playwright": "catalog:",
    "publint": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "vitest-browser-react": "^2.2.0"
  }
}
